# GitHub Copilot repository instructions

Use `AGENTS.md` as the canonical repository contract. Read it before editing, then load only the
scoped repository documents it routes for the current task. Do not duplicate or weaken those rules
here; when this adapter and `AGENTS.md` differ, `AGENTS.md` controls.
